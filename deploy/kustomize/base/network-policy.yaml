apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: readtheplan-smoke-deny-all
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: readtheplan-smoke
  policyTypes:
    - Ingress
    - Egress

