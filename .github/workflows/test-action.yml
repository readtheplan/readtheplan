name: Test GitHub Action

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test-action:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Run action on valid plan
        id: valid
        uses: ./
        with:
          plan-file: tests/fixtures/valid_plan.json
          fail-on-any-change: "false"

      - name: Assert valid action outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.valid.outputs['summary-json'] }}
          RESOURCE_CHANGE_COUNT: ${{ steps.valid.outputs['resource-change-count'] }}
          ACTION_COUNTS: ${{ steps.valid.outputs['action-counts'] }}
          RISK_COUNTS: ${{ steps.valid.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$RESOURCE_CHANGE_COUNT" = "3"
          test "$ACTION_COUNTS" = '{"create":1,"delete/create":1,"update":1}'
          test "$RISK_COUNTS" = '{"dangerous":1,"review":1,"safe":1}'

          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["resource_change_count"] == 3
          assert payload["actions"]["delete/create"] == 1
          assert payload["risks"]["dangerous"] == 1
          assert payload["changes"][0]["address"] == "aws_s3_bucket.logs"
          PY

      - name: Run action on Pulumi preview
        id: pulumi
        uses: ./
        with:
          tool: pulumi
          input-file: tests/fixtures/pulumi_preview_mixed.json
          framework: soc2

      - name: Assert Pulumi action outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.pulumi.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.pulumi.outputs['change-count'] }}
          ACTION_COUNTS: ${{ steps.pulumi.outputs['action-counts'] }}
          RISK_COUNTS: ${{ steps.pulumi.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "3"
          test "$ACTION_COUNTS" = '{}'
          test "$RISK_COUNTS" = '{"dangerous":1,"irreversible":1,"review":0,"safe":1}'

          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "pulumi"
          assert payload["decision"] == "block"
          assert payload["total_changes"] == 3
          assert any(item.startswith("rtp.control.soc2.") for item in payload["required_checks"])
          PY

      - name: Reject unsupported tool
        id: unsupported_tool
        uses: ./
        continue-on-error: true
        with:
          tool: unsupported
          input-file: tests/fixtures/valid_plan.json

      - name: Assert unsupported tool failed
        if: ${{ steps.unsupported_tool.outcome != 'failure' }}
        shell: bash
        run: |
          echo "Expected unsupported tool validation to fail"
          exit 1

      - name: Run action on GitHub Actions workflow
        id: github_actions
        uses: ./
        with:
          tool: github-actions
          input-file: tests/fixtures/github_actions_deploy.yml
          framework: soc2

      - name: Assert GitHub Actions workflow outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.github_actions.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.github_actions.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.github_actions.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "4"
          test "$RISK_COUNTS" = '{"dangerous":1,"irreversible":0,"review":2,"safe":1}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "github-actions"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Azure Pipelines
        id: azure_pipelines
        uses: ./
        with:
          tool: azure-pipelines
          input-file: tests/fixtures/azure_pipelines_deploy.yml
          framework: soc2

      - name: Assert Azure Pipelines outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.azure_pipelines.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.azure_pipelines.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.azure_pipelines.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "21"
          test "$RISK_COUNTS" = '{"dangerous":14,"irreversible":0,"review":7,"safe":0}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "azure-pipelines"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Bitbucket Pipelines
        id: bitbucket_pipelines
        uses: ./
        with:
          tool: bitbucket-pipelines
          input-file: tests/fixtures/bitbucket_pipelines_deploy.yml
          framework: soc2

      - name: Assert Bitbucket Pipelines outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.bitbucket_pipelines.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.bitbucket_pipelines.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.bitbucket_pipelines.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "27"
          test "$RISK_COUNTS" = '{"dangerous":15,"irreversible":0,"review":12,"safe":0}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "bitbucket-pipelines"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Buildkite pipeline
        id: buildkite
        uses: ./
        with:
          tool: buildkite
          input-file: tests/fixtures/buildkite_deploy.yml
          framework: soc2

      - name: Assert Buildkite outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.buildkite.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.buildkite.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.buildkite.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "18"
          test "$RISK_COUNTS" = '{"dangerous":8,"irreversible":0,"review":9,"safe":1}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "buildkite"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Atlantis configuration
        id: atlantis
        uses: ./
        with:
          tool: atlantis
          input-file: tests/fixtures/atlantis_risky.yaml
          framework: soc2

      - name: Assert Atlantis outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.atlantis.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.atlantis.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.atlantis.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "18"
          test "$RISK_COUNTS" = '{"dangerous":10,"irreversible":0,"review":7,"safe":1}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "atlantis"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Envoy configuration
        id: envoy
        uses: ./
        with:
          tool: envoy
          input-file: tests/fixtures/envoy_risky.yaml
          framework: soc2

      - name: Assert Envoy outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.envoy.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.envoy.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.envoy.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "20"
          test "$RISK_COUNTS" = '{"dangerous":9,"irreversible":0,"review":10,"safe":1}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "envoy"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Prometheus configuration
        id: prometheus
        uses: ./
        with:
          tool: prometheus
          input-file: tests/fixtures/prometheus_risky.yml
          framework: soc2

      - name: Assert Prometheus outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.prometheus.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.prometheus.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.prometheus.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "16"
          test "$RISK_COUNTS" = '{"dangerous":9,"irreversible":0,"review":7,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "prometheus"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Alertmanager configuration
        id: alertmanager
        uses: ./
        with:
          tool: alertmanager
          input-file: tests/fixtures/alertmanager_risky.yml
          framework: soc2

      - name: Assert Alertmanager outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.alertmanager.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.alertmanager.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.alertmanager.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "17"
          test "$RISK_COUNTS" = '{"dangerous":10,"irreversible":0,"review":7,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "alertmanager"
          assert payload["decision"] == "block"
          PY

      - name: Run action on OpenTelemetry Collector configuration
        id: otel_collector
        uses: ./
        with:
          tool: otel-collector
          input-file: tests/fixtures/otel_collector_risky.yml
          framework: soc2

      - name: Assert OpenTelemetry Collector outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.otel_collector.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.otel_collector.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.otel_collector.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "26"
          test "$RISK_COUNTS" = '{"dangerous":16,"irreversible":0,"review":8,"safe":2}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "otel-collector"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Traefik configuration
        id: traefik
        uses: ./
        with:
          tool: traefik
          input-file: tests/fixtures/traefik_risky.yml
          framework: soc2

      - name: Assert Traefik outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.traefik.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.traefik.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.traefik.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "23"
          test "$RISK_COUNTS" = '{"dangerous":17,"irreversible":0,"review":6,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "traefik"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Grafana provisioning
        id: grafana
        uses: ./
        with:
          tool: grafana
          input-file: tests/fixtures/grafana_provisioning_risky.yml
          framework: soc2

      - name: Assert Grafana outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.grafana.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.grafana.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.grafana.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "27"
          test "$RISK_COUNTS" = '{"dangerous":18,"irreversible":0,"review":9,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "grafana"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Vault configuration
        id: vault
        uses: ./
        with:
          tool: vault
          input-file: tests/fixtures/vault_risky.hcl
          framework: soc2

      - name: Assert Vault outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.vault.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.vault.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.vault.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "24"
          test "$RISK_COUNTS" = '{"dangerous":15,"irreversible":0,"review":9,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "vault"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Consul configuration
        id: consul
        uses: ./
        with:
          tool: consul
          input-file: tests/fixtures/consul_risky.hcl
          framework: soc2

      - name: Assert Consul outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.consul.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.consul.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.consul.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "28"
          test "$RISK_COUNTS" = '{"dangerous":19,"irreversible":0,"review":9,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "consul"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Loki configuration
        id: loki
        uses: ./
        with:
          tool: loki
          input-file: tests/fixtures/loki_risky.yml
          framework: soc2

      - name: Assert Loki outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.loki.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.loki.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.loki.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "41"
          test "$RISK_COUNTS" = '{"dangerous":23,"irreversible":0,"review":18,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "loki"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Caddyfile
        id: caddy
        uses: ./
        with:
          tool: caddy
          input-file: tests/fixtures/Caddyfile.risky
          framework: soc2

      - name: Assert Caddy outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.caddy.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.caddy.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.caddy.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "24"
          test "$RISK_COUNTS" = '{"dangerous":17,"irreversible":0,"review":7,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "caddy"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Terraform configuration
        id: terraform_config
        uses: ./
        with:
          tool: terraform-config
          input-file: tests/fixtures/terraform_config_risky.tf
          framework: soc2

      - name: Assert Terraform configuration outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.terraform_config.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.terraform_config.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.terraform_config.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "39"
          test "$RISK_COUNTS" = '{"dangerous":23,"irreversible":1,"review":15,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "terraform-config"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Terragrunt configuration
        id: terragrunt
        uses: ./
        with:
          tool: terragrunt
          input-file: tests/fixtures/terragrunt_risky.hcl
          framework: soc2

      - name: Assert Terragrunt outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.terragrunt.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.terragrunt.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.terragrunt.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "44"
          test "$RISK_COUNTS" = '{"dangerous":25,"irreversible":0,"review":19,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "terragrunt"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Helm template source
        id: helm
        uses: ./
        with:
          tool: helm
          input-file: tests/fixtures/helm_template_risky.yaml
          framework: soc2

      - name: Assert Helm outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.helm.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.helm.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.helm.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "13"
          test "$RISK_COUNTS" = '{"dangerous":6,"irreversible":0,"review":7,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "helm"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Kustomize source
        id: kustomize
        uses: ./
        with:
          tool: kustomize
          input-file: tests/fixtures/kustomization_risky.yml
          framework: soc2

      - name: Assert Kustomize outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.kustomize.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.kustomize.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.kustomize.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "29"
          test "$RISK_COUNTS" = '{"dangerous":15,"irreversible":0,"review":14,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "kustomize"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Crossplane source
        id: crossplane
        uses: ./
        with:
          tool: crossplane
          input-file: tests/fixtures/crossplane_risky.yml
          framework: soc2

      - name: Assert Crossplane outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.crossplane.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.crossplane.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.crossplane.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "55"
          test "$RISK_COUNTS" = '{"dangerous":33,"irreversible":1,"review":20,"safe":1}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "crossplane"
          assert payload["decision"] == "block"
          assert "rtp.check.recovery_plan" in payload["required_checks"]
          PY

      - name: Run action on Serverless Framework source
        id: serverless
        uses: ./
        with:
          tool: serverless
          input-file: tests/fixtures/serverless_framework_risky.yml
          framework: soc2

      - name: Assert Serverless Framework outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.serverless.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.serverless.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.serverless.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "47"
          test "$RISK_COUNTS" = '{"dangerous":24,"irreversible":0,"review":23,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "serverless"
          assert payload["decision"] == "block"
          PY

      - name: Run action on AWS SAM source
        id: sam
        uses: ./
        with:
          tool: sam
          input-file: tests/fixtures/sam_template_risky.yml
          framework: soc2

      - name: Assert AWS SAM outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.sam.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.sam.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.sam.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "44"
          test "$RISK_COUNTS" = '{"dangerous":30,"irreversible":0,"review":14,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "sam"
          assert payload["decision"] == "block"
          PY

      - name: Run action on Docker Compose
        id: docker_compose
        uses: ./
        with:
          tool: docker-compose
          input-file: tests/fixtures/docker_compose_risky.yml
          framework: soc2

      - name: Assert Docker Compose outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.docker_compose.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.docker_compose.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.docker_compose.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "12"
          test "$RISK_COUNTS" = '{"dangerous":9,"irreversible":0,"review":1,"safe":2}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "docker-compose"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Nomad plan
        id: nomad
        uses: ./
        with:
          tool: nomad
          input-file: tests/fixtures/nomad_plan_risky.json
          framework: soc2

      - name: Assert Nomad outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.nomad.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.nomad.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.nomad.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "10"
          test "$RISK_COUNTS" = '{"dangerous":6,"irreversible":0,"review":4,"safe":0}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "nomad"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Azure What-If
        id: azure
        uses: ./
        with:
          tool: azure
          input-file: tests/fixtures/azure_whatif_mixed.json
          framework: soc2

      - name: Assert Azure action outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.azure.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.azure.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.azure.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "5"
          test "$RISK_COUNTS" = '{"dangerous":1,"irreversible":1,"review":2,"safe":1}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "azure"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Flux manifests
        id: flux
        uses: ./
        with:
          tool: kubernetes
          input-file: tests/fixtures/flux_gitops_risky.yml
          framework: soc2

      - name: Assert Flux outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.flux.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.flux.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.flux.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "5"
          test "$RISK_COUNTS" = '{"dangerous":5,"irreversible":0,"review":0,"safe":0}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "kubernetes"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Tekton manifests
        id: tekton
        uses: ./
        with:
          tool: kubernetes
          input-file: tests/fixtures/tekton_risky.yml
          framework: soc2

      - name: Assert Tekton outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.tekton.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.tekton.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.tekton.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "7"
          test "$RISK_COUNTS" = '{"dangerous":6,"irreversible":0,"review":1,"safe":0}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "kubernetes"
          assert payload["decision"] == "block"
          assert "Tekton Task" in payload["pr_comment"]
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on popular Kubernetes controllers
        id: kubernetes_controllers
        uses: ./
        with:
          tool: kubernetes
          input-file: tests/fixtures/kubernetes_controllers_risky.yml
          framework: soc2

      - name: Assert Kubernetes controller outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.kubernetes_controllers.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.kubernetes_controllers.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.kubernetes_controllers.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "19"
          test "$RISK_COUNTS" = '{"dangerous":19,"irreversible":0,"review":0,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "kubernetes"
          assert payload["decision"] == "block"
          assert "Argo Workflow" in payload["pr_comment"]
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Packer inspect output
        id: packer
        uses: ./
        with:
          tool: packer
          input-file: tests/fixtures/packer_inspect_risky.txt
          framework: soc2

      - name: Assert Packer outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.packer.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.packer.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.packer.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "11"
          test "$RISK_COUNTS" = '{"dangerous":5,"irreversible":0,"review":4,"safe":2}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "packer"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Salt states
        id: salt
        uses: ./
        with:
          tool: salt
          input-file: tests/fixtures/salt_states_risky.sls
          framework: soc2

      - name: Assert Salt outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.salt.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.salt.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.salt.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "6"
          test "$RISK_COUNTS" = '{"dangerous":2,"irreversible":0,"review":3,"safe":1}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "salt"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Vagrantfile
        id: vagrant
        uses: ./
        with:
          tool: vagrant
          input-file: tests/fixtures/Vagrantfile.risky
          framework: soc2

      - name: Assert Vagrant outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.vagrant.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.vagrant.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.vagrant.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "17"
          test "$RISK_COUNTS" = '{"dangerous":12,"irreversible":0,"review":5,"safe":0}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "vagrant"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on cloud-init user-data
        id: cloud_init
        uses: ./
        with:
          tool: cloud-init
          input-file: tests/fixtures/cloud_init_risky.yml
          framework: soc2

      - name: Assert cloud-init outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.cloud_init.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.cloud_init.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.cloud_init.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "20"
          test "$RISK_COUNTS" = '{"dangerous":11,"irreversible":0,"review":8,"safe":1}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "cloud-init"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on Dockerfile
        id: dockerfile
        uses: ./
        with:
          tool: dockerfile
          input-file: tests/fixtures/Dockerfile.risky
          framework: soc2

      - name: Assert Dockerfile outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.dockerfile.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.dockerfile.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.dockerfile.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "20"
          test "$RISK_COUNTS" = '{"dangerous":9,"irreversible":0,"review":10,"safe":1}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "dockerfile"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on systemd unit
        id: systemd
        uses: ./
        with:
          tool: systemd
          input-file: tests/fixtures/systemd_risky.service
          framework: soc2

      - name: Assert systemd outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.systemd.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.systemd.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.systemd.outputs['risk-counts'] }}
        run: |
          set -euo pipefail

          test "$CHANGE_COUNT" = "30"
          test "$RISK_COUNTS" = '{"dangerous":20,"irreversible":0,"review":9,"safe":1}'
          python - <<'PY'
          import json
          import os

          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "systemd"
          assert payload["decision"] == "block"
          assert "rtp.control.soc2.CC8.1" in payload["required_checks"]
          PY

      - name: Run action on NGINX configuration
        id: nginx
        uses: ./
        with:
          tool: nginx
          input-file: tests/fixtures/nginx_risky.conf
          framework: soc2

      - name: Assert NGINX outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.nginx.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.nginx.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.nginx.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "19"
          test "$RISK_COUNTS" = '{"dangerous":9,"irreversible":0,"review":10,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "nginx"
          assert payload["decision"] == "block"
          PY

      - name: Run action on HAProxy configuration
        id: haproxy
        uses: ./
        with:
          tool: haproxy
          input-file: tests/fixtures/haproxy_risky.cfg
          framework: soc2

      - name: Assert HAProxy outputs
        shell: bash
        env:
          SUMMARY_JSON: ${{ steps.haproxy.outputs['summary-json'] }}
          CHANGE_COUNT: ${{ steps.haproxy.outputs['change-count'] }}
          RISK_COUNTS: ${{ steps.haproxy.outputs['risk-counts'] }}
        run: |
          set -euo pipefail
          test "$CHANGE_COUNT" = "19"
          test "$RISK_COUNTS" = '{"dangerous":11,"irreversible":0,"review":8,"safe":0}'
          python - <<'PY'
          import json
          import os
          payload = json.loads(os.environ["SUMMARY_JSON"])
          assert payload["adapter"] == "haproxy"
          assert payload["decision"] == "block"
          PY

      - name: Fail on invalid plan JSON
        id: invalid
        uses: ./
        continue-on-error: true
        with:
          plan-file: tests/fixtures/invalid_plan.json

      - name: Assert invalid plan failed
        if: ${{ steps.invalid.outcome != 'failure' }}
        shell: bash
        run: |
          echo "Expected invalid plan analysis to fail"
          exit 1

      - name: Fail on missing plan file
        id: missing
        uses: ./
        continue-on-error: true
        with:
          plan-file: tests/fixtures/does-not-exist.json

      - name: Assert missing plan failed
        if: ${{ steps.missing.outcome != 'failure' }}
        shell: bash
        run: |
          echo "Expected missing plan file to fail"
          exit 1

      - name: Fail when changes are present and fail-on-any-change is true
        id: fail_on_changes
        uses: ./
        continue-on-error: true
        with:
          plan-file: tests/fixtures/valid_plan.json
          fail-on-any-change: "true"

      - name: Assert fail-on-any-change failed
        if: ${{ steps.fail_on_changes.outcome != 'failure' }}
        shell: bash
        run: |
          echo "Expected fail-on-any-change to fail for a plan with changes"
          exit 1

      - name: Fail on dangerous threshold
        id: fail_on_threshold
        uses: ./
        continue-on-error: true
        with:
          plan-file: tests/fixtures/valid_plan.json
          fail-on-threshold: "dangerous"

      - name: Assert fail-on-threshold failed
        if: ${{ steps.fail_on_threshold.outcome != 'failure' }}
        shell: bash
        run: |
          echo "Expected fail-on-threshold to fail for a dangerous plan"
          exit 1
