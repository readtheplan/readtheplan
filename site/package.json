{
  "name": "readtheplan-site",
  "version": "0.5.0",
  "private": true,
  "description": "Static onboarding app for readtheplan.dev.",
  "scripts": {
    "test": "node scripts/validate.js && node analysis/chat-contract.test.mjs && node analysis/interaction-contract.test.mjs && node analysis/design-system-contract.test.mjs",
    "build": "node scripts/build.js && node analysis/build-contract.test.mjs && node analysis/rendered-route-contract.test.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6"
  },
  "engines": {
    "node": ">=18"
  }
}
