{
  "name": "readtheplan-site",
  "version": "0.3.0",
  "private": true,
  "description": "Static onboarding app for readtheplan.dev.",
  "scripts": {
    "test": "node scripts/validate.js && node analysis/chat-contract.test.mjs",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=18"
  }
}
