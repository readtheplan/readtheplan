---
- name: fixture-review-rulebook
  hosts: localhost
  sources:
    - eda.builtin.range:
        limit: 5
      filters:
        - json_filter:
            include_keys:
              - status
  rules:
    - name: fixture-test-event
      condition: event.i == 1
      action:
        none:
