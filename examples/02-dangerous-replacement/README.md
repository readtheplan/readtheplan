# Dangerous replacement example

This signed envelope is for demonstration only. Production usage signs with the deployer's real OIDC identity.

The committed `evidence.json` uses a deterministic mock bundle named `readtheplan_examples_fixture_v1` so the regeneration script can reproduce it exactly. Replace it with a real Sigstore-signed envelope when testing production verification.
